{
  "name": "archiverse-theia-monorepo",
  "private": true,
  "description": "Monorepo root for Archiverse Theia",
  "workspaces": [
    "packages/*",
    "services/*",
    "hosts/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build --stream",
    "clean": "lerna run clean --parallel"
  },
  "devDependencies": {
    "lerna": "^8.2.1"
  }
}
