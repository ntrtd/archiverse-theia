import { IntermediateRepresentation } from "../../ir-types.js";
/**
 * Renders GraphQL SDL from the Intermediate Representation.
 * @param ir The Intermediate Representation.
 * @param logPrefix The prefix string for logging messages (unused in placeholder).
 */
export declare function renderGraphQLSDL(ir: IntermediateRepresentation, logPrefix?: string): string;
//# sourceMappingURL=index.d.ts.map