import { IntermediateRepresentation } from "../../ir-types.js";
/**
 * Renders OData v4 CSDL XML ($metadata) from the Intermediate Representation.
 * @param ir The Intermediate Representation.
 * @param logPrefix The prefix string for logging messages.
 */
export declare function renderODataMetadata(ir: IntermediateRepresentation, logPrefix?: string): string;
//# sourceMappingURL=index.d.ts.map